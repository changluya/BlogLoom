# 贡献指南

感谢你对 BlogLoom 的关注！无论是提交问题、完善文档，还是贡献代码，我们都非常欢迎。

在参与贡献之前，请先阅读本指南，它可以帮助你更快地让改动被合并。

## 行为准则

请保持友善、尊重与耐心，围绕技术问题展开讨论，不进行人身攻击、广告推广或与项目无关的争论。

## 我可以如何贡献

- **报告问题**：通过 [Issue](../../issues) 反馈 Bug、文档错误或使用疑问。
- **提出建议**：通过 [Issue](../../issues) 提出新功能或改进想法。
- **贡献代码**：修复 Bug、实现功能或优化文档，并通过 Pull Request 提交。
- **完善文档**：修正 README、文档站或代码注释中的错误与遗漏。

## 报告问题

提交 Issue 前，请先搜索是否已有相同或相似的问题，避免重复。提交时建议使用仓库提供的 Issue 模板，并尽量包含：

1. **问题描述**：清晰说明遇到了什么问题。
2. **复现步骤**：列出可以稳定复现问题的操作步骤。
3. **期望行为与实际行为**：说明你期望的结果与实际情况。
4. **环境信息**：涉及的模块、版本、JDK / Node.js 版本、数据库版本、浏览器与部署方式。
5. **日志或截图**：相关的报错日志、堆栈或界面截图。

> 如果是安全相关问题，请**不要**提交公开 Issue，而应按照 [SECURITY.md](./SECURITY.md) 中的方式私下报告。

## 开发环境

本地开发的环境要求与启动步骤见 README 的[本地开发](./README.md#本地开发)一节，主要包含：

- JDK 8+、Maven 3.6+
- Node.js 16+ 与 npm
- MySQL 5.7+ 或 MySQL 8

各模块职责：

| 模块 | 说明 |
| --- | --- |
| `blog-backend` | Spring Boot 后端服务与 REST API |
| `blog-cms-ui` | Vue 内容管理后台 |
| `blog-view-ui` | Vue 公开博客前台 |
| `docs-website` | 官网文档站 |

## 分支与提交

1. 从 `master` 拉取最新代码，创建独立的功能分支，例如 `feat/xxx`、`fix/xxx`、`docs/xxx`。
2. 提交信息建议使用 `feat`、`fix`、`docs`、`refactor`、`chore` 等前缀，简明描述改动内容。
3. 保持提交聚焦，一次提交尽量只处理一件事，避免混入无关改动。

## 代码规范

- 遵循所在模块已有的代码风格与目录结构，优先复用现有工具与组件。
- 后端保持 Controller / Service / Mapper 的分层约定，接口与异常处理保持一致。
- 前端沿用现有组件、状态管理与路由组织方式，不随意引入新的依赖或框架。
- 新增或修改代码时，同步补充必要的注释与文档。

## 数据库变更

- 数据库结构或初始数据发生变化时，必须同步维护 `sql/` 下的全量 SQL 与必要的增量 SQL。
- 增量 SQL 按版本归档，确保已部署实例可以平滑升级，不破坏既有数据。

## 提交 Pull Request

提交 PR 时，请使用仓库提供的 PR 模板，并确保：

1. 已在本地分别验证涉及模块可以正常构建（后端 `mvn -B -ntp verify`，前端 `npm run build`）。
2. 数据库变更已同步维护全量 SQL 与增量 SQL。
3. 未提交本地日志、上传文件、真实账号密码、令牌密钥或对象存储密钥等敏感信息。
4. 在变更说明中写清影响模块、验证方式和兼容性注意事项。
5. 如涉及界面改动，附上相关截图。

CI 会在提交 PR 后自动运行后端与前后台的构建校验，请确保全部通过。

## 许可证

向本项目提交的贡献将按 [MIT License](./LICENSE) 授权。
